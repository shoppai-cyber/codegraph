{
  "$schema": "https://codegraph.dev/schemas/unity-invocation-table.v0.json",
  "name": "unity-runtime-invocation-table",
  "version": "0.4.0",
  "target": "Unity 2021 LTS – 6.x; runtime + editor C# scripting; FishNet 4.x and Mirror 96.x networking (Tier 2, gated)",
  "status": "Fable-reviewed v1 scope plus the FishNet and Mirror Tier-2 networking sections. Consumed lists are grounded in the local Unity 6.5 corpus and FABLE-SCOPE-DECISIONS.md (Tier 1), the FishNet 4.7.2 corpus verified against the validation project's 4.6.20 source (Tier 2, see resolver-facts/FISHNET-RESOLVER-FACTS.md), and the Mirror 96.10.3 corpus verified against Weaver source (Tier 2, see resolver-facts/MIRROR-RESOLVER-FACTS.md); pending rows are explicitly marked or quarantined. Runtime consumption must stay inside the consumed sections.",
  "consumed": {
    "note": "Sections here are read by unity.ts at runtime; the fields consumed are named per section. Everything under catalogNotes is documentation-only provenance. This mirrors blender-invocation-table.json's consumed/catalogNotes split.",
    "detection": "DOCUMENTATION-ONLY despite sitting in this manifest: the marker list is hardcoded in unity.ts detect()/hasConcreteUnitySourceSignal — editing this section changes nothing at runtime. Describes project-level sufficient markers plus source evidence rules used to recognize a Unity project.",
    "hostInvokedBases": "key set (the host-invoked-base whitelist); <base>.hostInvokedMethods (engine/editor calls these by reflection, so they look dead)",
    "serializationAttributes": "attribute set → field kept live + its declared type emits a references edge",
    "attributeEntryPoints": "attribute → the decorated method/class is a host-invoked entry point (kept live); menu/path string captured where present",
    "typeReferenceAttributes": "attribute → typeof(X)/type argument emits a references edge to X",
    "stringInvokedCallSites": "DOCUMENTATION-ONLY despite sitting in this manifest: the call-site families (SendMessage/Invoke/StartCoroutine/ContextMenuItem) are hardcoded in unity.ts processStringCalls/processContextMenuItems — editing this section changes nothing at runtime. Describes callsite API → its string-literal method-name argument links to a sibling method (references). This mirrors how blender-invocation-table.json documents its hardcoded call-site behavior under catalogNotes.",
    "fishnet": "gated FishNet 4.x networking rules: fishnet.gate (per-file using-based gate), fishnet.hostInvokedBases.NetworkBehaviour.hostInvokedMethods (+includesMonoBehaviourMessages union), and fishnet.attributeEntryPoints.methodAttributes (RPC/prediction entry points). Every fishnet row only fires when fishnet.gate is open in that file.",
    "mirror": "gated Mirror 96.x networking rules: mirror.gate (per-file using/FQ-base gate with NGO/FishNet/Fusion disqualifiers), mirror.hostInvokedBases.NetworkBehaviour.hostInvokedMethods (+includesMonoBehaviourMessages union), mirror.attributeEntryPoints.methodAttributes (Command/ClientRpc/TargetRpc entry points), mirror.syncVarFields (non-static [SyncVar] field liveness), and mirror.syncVarHooks (hook-string method resolution). Every mirror row only fires when mirror.gate is open in that file."
  },
  "sourcePriority": [
    "kb/gamedev/unity (to be built — Unity Scripting API + Manual mirror)",
    "Unity MonoBehaviour message list (official Scripting API)",
    "real-project probes (Unity-MP-Course-Project + small/med/large validation repos)",
    "codegraph repository source (blender.ts pattern)",
    "official Unity documentation"
  ],
  "detection": {
    "note": "detect() returns true when the C# project is a Unity project. Project/file-layout markers are sufficient; source fallback requires concrete Unity base/attribute evidence. Bare using UnityEngine/UnityEditor is supporting context only and is not independently sufficient.",
    "markers": [
      "a file named ProjectSettings/ProjectVersion.txt exists in the project",
      "Packages/manifest.json references com.unity.* packages",
      "*.asmdef assembly-definition files present",
      "a .cs file contains a concrete Unity host base or v1 Unity attribute in source",
      "`using UnityEngine;` or `using UnityEditor;` may support detection only when paired with concrete source evidence",
      ".meta sidecar files accompany source assets"
    ]
  },
  "hostInvokedBases": {
    "note": "A class is host-invoked only when its class-statement base is structurally one of these. Magic method names on non-host classes emit nothing. Method sets are closed, docs-grounded v1 sets; no convention or substring matching.",
    "MonoBehaviour": {
      "hostInvokedMethods": [
        "Awake",
        "FixedUpdate",
        "LateUpdate",
        "OnAnimatorIK",
        "OnAnimatorMove",
        "OnApplicationFocus",
        "OnApplicationPause",
        "OnApplicationQuit",
        "OnAudioFilterRead",
        "OnBecameInvisible",
        "OnBecameVisible",
        "OnChildRectTransformDimensionsChange",
        "OnCollisionEnter",
        "OnCollisionEnter2D",
        "OnCollisionExit",
        "OnCollisionExit2D",
        "OnCollisionStay",
        "OnCollisionStay2D",
        "OnControllerColliderHit",
        "OnDestroy",
        "OnDisable",
        "OnDrawGizmos",
        "OnDrawGizmosSelected",
        "OnEnable",
        "OnGUI",
        "OnJointBreak",
        "OnJointBreak2D",
        "OnMouseDown",
        "OnMouseDrag",
        "OnMouseEnter",
        "OnMouseExit",
        "OnMouseOver",
        "OnMouseUp",
        "OnMouseUpAsButton",
        "OnParticleCollision",
        "OnParticleSystemStopped",
        "OnParticleTrigger",
        "OnParticleUpdateJobScheduled",
        "OnPostRender",
        "OnPreCull",
        "OnPreRender",
        "OnRenderImage",
        "OnRenderObject",
        "OnTransformChildrenChanged",
        "OnTransformParentChanged",
        "OnTriggerEnter",
        "OnTriggerEnter2D",
        "OnTriggerExit",
        "OnTriggerExit2D",
        "OnTriggerStay",
        "OnTriggerStay2D",
        "OnValidate",
        "OnWillRenderObject",
        "Reset",
        "Start",
        "Update"
      ],
      "detection": "base list includes UnityEngine.MonoBehaviour; same-file base chains are allowed when proven in-file; cross-file base chains emit nothing"
    },
    "ScriptableObject": {
      "hostInvokedMethods": ["Awake", "OnDestroy", "OnDisable", "OnEnable", "OnValidate", "Reset"],
      "detection": "base includes UnityEngine.ScriptableObject"
    },
    "StateMachineBehaviour": {
      "hostInvokedMethods": [
        "OnStateEnter",
        "OnStateExit",
        "OnStateIK",
        "OnStateMove",
        "OnStateUpdate",
        "OnStateMachineEnter",
        "OnStateMachineExit",
        "Awake",
        "OnDestroy",
        "OnDisable",
        "OnEnable",
        "OnValidate",
        "Reset"
      ],
      "detection": "base includes UnityEngine.StateMachineBehaviour"
    },
    "Editor": {
      "hostInvokedMethods": [
        "Awake",
        "OnEnable",
        "OnDisable",
        "OnDestroy",
        "OnValidate",
        "Reset",
        "OnSceneGUI",
        "HasFrameBounds",
        "OnGetFrameBounds",
        "OnInspectorGUI",
        "CreateInspectorGUI",
        "HasPreviewGUI",
        "OnPreviewGUI",
        "OnInteractivePreviewGUI",
        "OnPreviewSettings",
        "GetPreviewTitle",
        "GetInfoString",
        "RenderStaticPreview",
        "RequiresConstantRepaint",
        "UseDefaultMargins"
      ],
      "detection": "base includes UnityEditor.Editor"
    },
    "EditorWindow": {
      "hostInvokedMethods": [
        "Awake",
        "CreateGUI",
        "OnBecameInvisible",
        "OnBecameVisible",
        "OnDestroy",
        "OnDisable",
        "OnEnable",
        "OnFocus",
        "OnGUI",
        "OnHierarchyChange",
        "OnInspectorUpdate",
        "OnLostFocus",
        "OnProjectChange",
        "OnSelectionChange",
        "OnValidate",
        "Reset",
        "Update"
      ],
      "detection": "base includes UnityEditor.EditorWindow"
    },
    "PropertyDrawer": {
      "hostInvokedMethods": ["CreatePropertyGUI", "GetPropertyHeight", "OnGUI"],
      "detection": "base includes UnityEditor.PropertyDrawer"
    },
    "DecoratorDrawer": {
      "hostInvokedMethods": ["CreatePropertyGUI", "GetHeight", "OnGUI"],
      "detection": "base includes UnityEditor.DecoratorDrawer",
      "note": "CreatePropertyGUI validated: listed on the DecoratorDrawer page in the local Unity 6.5 corpus."
    },
    "AssetPostprocessor": {
      "hostInvokedMethods": [
        "OnAssignMaterialModel",
        "OnPostprocessAllAssets",
        "OnPostprocessAnimation",
        "OnPostprocessAssetbundleNameChanged",
        "OnPostprocessAudio",
        "OnPostprocessCubemap",
        "OnPostprocessGameObjectWithAnimatedUserProperties",
        "OnPostprocessGameObjectWithUserProperties",
        "OnPostprocessMaterial",
        "OnPostprocessMeshHierarchy",
        "OnPostprocessModel",
        "OnPostprocessPrefab",
        "OnPostprocessSpeedTree",
        "OnPostprocessSprites",
        "OnPostprocessTexture",
        "OnPostprocessTexture2DArray",
        "OnPostprocessTexture3D",
        "OnPreprocessAnimation",
        "OnPreprocessAsset",
        "OnPreprocessAudio",
        "OnPreprocessCameraDescription",
        "OnPreprocessLightDescription",
        "OnPreprocessMaterialDescription",
        "OnPreprocessModel",
        "OnPreprocessSpeedTree",
        "OnPreprocessTexture",
        "GetPostprocessOrder",
        "GetVersion"
      ],
      "detection": "base includes UnityEditor.AssetPostprocessor",
      "note": "Closed docs-grounded set; no OnPostprocess<Type> convention matching in v1."
    },
    "AssetModificationProcessor": {
      "hostInvokedMethods": [
        "CanOpenForEdit",
        "FileModeChanged",
        "IsOpenForEdit",
        "MakeEditable",
        "OnWillCreateAsset",
        "OnWillDeleteAsset",
        "OnWillMoveAsset",
        "OnWillSaveAssets"
      ],
      "detection": "base includes UnityEditor.AssetModificationProcessor",
      "note": "Hooks are static editor callbacks; non-static declarations emit nothing."
    }
  },
  "serializationAttributes": {
    "note": "A field bearing one of these is set by the engine/inspector via serialization: keep it live and emit a references edge to its declared type when the type is a plain local/custom non-builtin name.",
    "attributes": ["SerializeField", "SerializeReference"],
    "referenceKind": "references (to the field's declared type)",
    "resolverNotes": [
      "[field: SerializeField] on an auto-property emits a liveness node referencing the PROPERTY symbol; never fabricate a backing-field node; emit nothing if the property symbol is unavailable.",
      "Public fields of a [Serializable]/MonoBehaviour/ScriptableObject type are also serialized with no attribute, but detecting that requires Unity serializability rules and owner context; deferred.",
      "Skip primitive, builtin, System.*, UnityEngine.*-qualified, and known external field types."
    ]
  },
  "attributeEntryPoints": {
    "note": "The decorated method or class is invoked by Unity/editor infrastructure, not by an in-code caller. Attribute matching accepts short and Attribute-suffixed forms with optional UnityEngine./UnityEditor. qualification; exact token only, no substring matches.",
    "methodAttributes": [
      { "attribute": "RuntimeInitializeOnLoadMethod", "scope": "static method", "invokedBy": "runtime bootstrap", "requiresStatic": true },
      { "attribute": "InitializeOnLoadMethod", "scope": "static method", "invokedBy": "editor load", "requiresStatic": true },
      { "attribute": "MenuItem", "scope": "static method", "invokedBy": "editor menu", "pathArg": "first string literal", "requiresStatic": true },
      { "attribute": "ContextMenu", "scope": "instance method", "invokedBy": "inspector context menu", "pathArg": "first string literal", "requiresInstance": true },
      { "attribute": "PostProcessBuild", "scope": "static method", "invokedBy": "build pipeline", "requiresStatic": true, "status": "corpus-verified", "corpusPage": "Callbacks.PostProcessBuildAttribute" },
      { "attribute": "PostProcessScene", "scope": "static method", "invokedBy": "build pipeline", "requiresStatic": true, "status": "corpus-verified", "corpusPage": "Callbacks.PostProcessSceneAttribute" },
      { "attribute": "DidReloadScripts", "scope": "static method", "invokedBy": "editor after recompile", "requiresStatic": true, "status": "corpus-verified", "corpusPage": "Callbacks.DidReloadScripts" },
      { "attribute": "OnOpenAsset", "scope": "static method", "invokedBy": "editor asset open", "requiresStatic": true, "status": "corpus-verified", "corpusPage": "Callbacks.OnOpenAssetAttribute" },
      { "attribute": "DrawGizmo", "scope": "static method", "invokedBy": "editor gizmo pass", "requiresStatic": true, "optionalTypeofReference": true, "note": "typeof(X) ref only when an explicit typeof argument names a local/custom type; no first-parameter type inference in v1." }
    ],
    "classAttributes": [
      { "attribute": "InitializeOnLoad", "scope": "class", "invokedBy": "editor load runs the static constructor; keep the static ctor live" },
      { "attribute": "CreateAssetMenu", "scope": "class (ScriptableObject)", "invokedBy": "editor Create menu", "pathArg": "menuName" },
      { "attribute": "AddComponentMenu", "scope": "class (Component)", "pathArg": "first string literal" }
    ],
    "referenceKind": "entry-point liveness (no target); pathArg strings captured for provenance, not link resolution in v1"
  },
  "typeReferenceAttributes": {
    "note": "The attribute's typeof(X) argument emits a references edge to X. Skip primitive, builtin, System.*, UnityEngine.*-qualified, and known external types. DrawGizmo is a method entry point with optional typeof reference and lives under attributeEntryPoints.",
    "attributes": [
      { "attribute": "RequireComponent", "arg": "typeof(X) (1–3)", "linksTo": "component type X" },
      { "attribute": "CustomEditor", "arg": "typeof(X)", "linksTo": "the edited type X" },
      { "attribute": "CustomPropertyDrawer", "arg": "typeof(X)", "linksTo": "the drawn type/attribute X" }
    ],
    "referenceKind": "references"
  },
  "stringInvokedCallSites": {
    "note": "Preconditions for every call-site rule: (1) containing class provably host-based in the SAME file (direct or same-file-chained Unity host base); (2) single string LITERAL or bare-identifier nameof(M) — dotted nameof, concatenation, interpolation, variables, empty strings emit nothing; (3) exactly ONE same-class method matches — overload ambiguity emits NOTHING.",
    "callSites": [
      { "id": "send-message", "apiPattern": "Component.SendMessage(\"M\", ...) / BroadcastMessage / SendMessageUpwards", "argument": "first string literal or bare nameof(M)", "linksTo": "unique same-class sibling method only when receiver is implicit, this, or gameObject; any other receiver emits nothing; never project-wide name linking", "referenceKind": "references" },
      { "id": "invoke", "apiPattern": "MonoBehaviour.Invoke(\"M\", t) / InvokeRepeating / CancelInvoke / IsInvoking", "argument": "first string literal or bare nameof(M)", "linksTo": "unique same-class sibling method; receiver must be implicit or this", "referenceKind": "references" },
      { "id": "start-coroutine-string", "apiPattern": "StartCoroutine(\"M\") / StopCoroutine(\"M\")", "argument": "string literal or bare nameof(M)", "linksTo": "unique same-class coroutine method; receiver must be implicit or this", "referenceKind": "references", "note": "StartCoroutine(M()) is a normal static call; do not special-case it here." },
      { "id": "context-menu-item", "apiPattern": "[ContextMenuItem(label, \"MethodName\")] on a field", "argument": "second string literal or bare nameof(M)", "linksTo": "unique same-class method on the declaring host-based class", "referenceKind": "references" }
    ]
  },
  "fishnet": {
    "note": "FishNet (Fish-Networking) 4.x consumed rules — Tier 2. NetworkBehaviour, ServerRpc, and TargetRpc collide by name with Unity Netcode for GameObjects (NGO) and Mirror, so EVERY fishnet row is gated per file (see gate). Facts are corpus-verified against FishNet 4.7.2 and re-verified against the validation project's 4.6.20 source (stable since 4.0). Runtime consumption reads gate, hostInvokedBases.NetworkBehaviour.hostInvokedMethods (unioned with MonoBehaviour messages), and attributeEntryPoints.methodAttributes. Full provenance: resolver-facts/FISHNET-RESOLVER-FACTS.md.",
    "gate": {
      "note": "A fishnet row fires in a file ONLY when BOTH halves hold. EVIDENCE half: the file has a real `using FishNet;` / `using FishNet.<...>;` namespace import (alias directives `using FishNet = ...;` do NOT count), OR a parsed class BASE written fully-qualified as `FishNet.Object.NetworkBehaviour` (occurrences of that token outside a base clause do NOT count). EXCLUSION half: the file has NO `using Unity.Netcode<...>`, NO `using Mirror<...>`, and NO `using Fusion<...>` — any competing stack makes the colliding NetworkBehaviour / ServerRpc / TargetRpc tokens ambiguous, so emit nothing even when FishNet evidence is also present; exclusion always wins over evidence. Applied uniformly to every fishnet row (host bases and attribute entry points alike). Directive/base evidence is read from comment-stripped, string-masked source, so a commented or string-literal using never counts.",
      "requiresAnyUsingPrefix": ["FishNet"],
      "fullyQualifiedBaseAlternative": "FishNet.Object.NetworkBehaviour",
      "disqualifyingUsingPrefixes": ["Unity.Netcode", "Mirror", "Fusion"]
    },
    "hostInvokedBases": {
      "note": "NetworkBehaviour is a GATED host base (see gate) — deliberately NOT in the top-level ungated hostInvokedBases whitelist, because the bare `NetworkBehaviour` token is shared by NGO, Mirror, and FishNet. Its host-method set is the 10 public virtual FishNet lifecycle callbacks UNIONED with the full MonoBehaviour message set: FishNet's `NetworkBehaviour : MonoBehaviour` is a framework-guaranteed chain (verified in 4.6.20 source), so Update/Awake/OnTriggerEnter/etc. are engine-invoked on it too. The union is materialised by the resolver from the MonoBehaviour entry via includesMonoBehaviourMessages, so the MonoBehaviour list stays the single source of truth (no drift).",
      "NetworkBehaviour": {
        "hostInvokedMethods": [
          "OnStartNetwork",
          "OnStopNetwork",
          "OnStartServer",
          "OnStopServer",
          "OnStartClient",
          "OnStopClient",
          "OnSpawnServer",
          "OnDespawnServer",
          "OnOwnershipServer",
          "OnOwnershipClient"
        ],
        "includesMonoBehaviourMessages": true,
        "detection": "base list includes NetworkBehaviour (short form under an open gate, or fully-qualified FishNet.Object.NetworkBehaviour); same-file base chains are allowed when proven in-file; cross-file base chains emit nothing — identical to the Tier-1 MonoBehaviour rule",
        "note": "The 10 public virtual callbacks from FishNet 4.7.2 (corpus tier=free). Excludes the internal *_Internal variants and OnValidate (already a MonoBehaviour message, protected not public). OnSpawnServer/OnDespawnServer/OnOwnershipServer/OnOwnershipClient take a NetworkConnection argument; the resolver matches by name only."
      }
    },
    "attributeEntryPoints": {
      "note": "The decorated INSTANCE method is invoked by FishNet's networking/prediction layer, not by an in-code caller — entry-point liveness (self-reference only, no target). Requires the declaring class provably NetworkBehaviour-based in the SAME file AND the file gate open. Short and Attribute-suffixed forms match, same token rules as Tier-1. Static methods bearing these attributes emit nothing.",
      "methodAttributes": [
        { "attribute": "ServerRpc", "scope": "instance method", "invokedBy": "client→server remote call", "requiresInstance": true, "collidesWith": "Unity.Netcode ServerRpcAttribute (gated out by disqualifyingUsingPrefixes)" },
        { "attribute": "ObserversRpc", "scope": "instance method", "invokedBy": "server→all-observers remote call", "requiresInstance": true },
        { "attribute": "TargetRpc", "scope": "instance method", "invokedBy": "server→single-client remote call", "requiresInstance": true, "collidesWith": "Mirror TargetRpcAttribute (gated out by disqualifyingUsingPrefixes)" },
        { "attribute": "Replicate", "scope": "instance method", "invokedBy": "client-side prediction input tick", "requiresInstance": true },
        { "attribute": "Reconcile", "scope": "instance method", "invokedBy": "client-side prediction reconcile", "requiresInstance": true }
      ],
      "referenceKind": "entry-point liveness (no target); the RPC/prediction method is kept live via a self-reference"
    },
    "excludedRows": {
      "note": "Present in FishNet 4.7.2/4.6.20 but deliberately NOT consumed as liveness rows. Recorded here with reasons per the emit-nothing policy.",
      "guardAttributes": {
        "rows": ["Server", "Client"],
        "reason": "ServerAttribute/ClientAttribute are GUARD attributes ('Prevents this method from running if server/client is not active') — early-return wrappers around a normal method body, NOT framework entry points. They do not make a method host-invoked, so no liveness row.",
        "corpusSummaries": {
          "ServerAttribute": "Prevents a method from running if server is not active. Can only be used inside a NetworkBehaviour.",
          "ClientAttribute": "Prevents this method from running if client is not active."
        }
      },
      "legacySyncAttributes": {
        "rows": ["SyncVar", "SyncObject"],
        "reason": "SyncVarAttribute/SyncObjectAttribute are corpus is_obsolete=1 (legacy v3-style). Modern FishNet uses SyncVar<T>/SyncList<T> field TYPES with OnChange events, not attributes. Deferred unless a validation corpus shows real attribute usage.",
        "corpusObsolete": true
      },
      "unifiedRpc": {
        "rows": ["Rpc"],
        "reason": "FishNet 4.x also ships a unified RpcAttribute ([Rpc] with a RunLocally/communication direction), corpus tier=free. Left deferred for v1: it is not in the maintainer-specified v1 attribute set and the bare `Rpc` token also collides with NGO's [Rpc] (SendTo...). Candidate for a later addition under the same gate."
      }
    },
    "eventSubscriptions": {
      "note": "SyncType OnChange handlers (`_health.OnChange += OnHealthChanged;`), TimeManager event handlers (`base.TimeManager.OnTick += Tm_OnTick;`), and RegisterBroadcast handlers (`clientManager.RegisterBroadcast<T>(OnBroadcast);`) are method-group references, NOT FishNet-specific dispatch. Investigation finding: codegraph's general C# function-as-value capture (#756, CSHARP_SPEC in src/extraction/function-ref.ts — assignment_expression `+=` RHS and bare-identifier call arguments) ALREADY captures these handlers and resolves them to same-file method nodes as `references` edges via matchFunctionRef. No FishNet-specific bridge is added in this phase; it would double-emit. Coverage bound: the handler must be a same-file method that survives the function-ref gate and resolves uniquely — cross-file handlers and non-self member-value receivers stay uncovered (deliberately, per emit-nothing).",
      "resolverBehavior": "Emit nothing new; rely on the existing general C# function_ref mechanism.",
      "syncTypesWithOnChange": ["SyncVar", "SyncList", "SyncDictionary", "SyncHashSet", "SyncTimer", "SyncStopwatch", "ComponentStateSync", "StructySync"],
      "timeManagerEvents": ["OnTick", "OnPostTick", "OnPreTick", "OnUpdate", "OnLateUpdate", "OnFixedUpdate", "OnPostPhysicsSimulation", "OnPrePhysicsSimulation", "OnRoundTripTimeUpdated"]
    }
  },
  "mirror": {
    "note": "Mirror (MirrorNetworking) v96.x consumed rules — Tier 2. NetworkBehaviour, ClientRpc, and TargetRpc collide by name with Unity Netcode for GameObjects (NGO), FishNet, and Photon Fusion, so EVERY mirror row is gated per file (see gate). Facts are corpus-verified against Mirror v96.10.3 (mirror_docs.sqlite). Runtime consumption reads gate, hostInvokedBases.NetworkBehaviour.hostInvokedMethods (unioned with MonoBehaviour messages), attributeEntryPoints.methodAttributes, syncVarFields, and syncVarHooks. Full provenance: mirror-docs/96.10.3/resolver-facts/MIRROR-RESOLVER-FACTS.md.",
    "gate": {
      "note": "A mirror row fires in a file ONLY when BOTH halves hold. EVIDENCE half: the file has a real `using Mirror;` / `using Mirror.<...>;` namespace import (alias directives `using Mirror = ...;` do NOT count), OR a parsed class BASE written fully-qualified as `Mirror.NetworkBehaviour` (occurrences of that token outside a base clause do NOT count). EXCLUSION half: the file has NO `using Unity.Netcode<...>`, NO `using FishNet<...>`, and NO `using Fusion<...>` — a competing stack makes the colliding tokens ambiguous, so emit nothing even when Mirror evidence is also present; exclusion always wins over evidence. Applied uniformly to every mirror row. Directive/base evidence is read from comment-stripped, string-masked source.",
      "requiresAnyUsingPrefix": ["Mirror"],
      "fullyQualifiedBaseAlternative": "Mirror.NetworkBehaviour",
      "disqualifyingUsingPrefixes": ["Unity.Netcode", "FishNet", "Fusion"]
    },
    "hostInvokedBases": {
      "note": "NetworkBehaviour is a GATED host base — the bare token is shared by NGO, FishNet, and Photon Fusion. Host-method set = the 12 framework-invoked overridable methods below UNIONED with the full MonoBehaviour message set (Mirror's `NetworkBehaviour : MonoBehaviour` is framework-guaranteed; union materialised via includesMonoBehaviourMessages).",
      "NetworkBehaviour": {
        "hostInvokedMethods": [
          "OnStartServer", "OnStopServer",
          "OnStartClient", "OnStopClient",
          "OnStartLocalPlayer", "OnStopLocalPlayer",
          "OnStartAuthority", "OnStopAuthority",
          "OnSerialize", "OnDeserialize",
          "SerializeSyncVars", "DeserializeSyncVars"
        ],
        "includesMonoBehaviourMessages": true,
        "detection": "identical to the fishnet NetworkBehaviour rule: short form under an open gate, or fully-qualified Mirror.NetworkBehaviour as a parsed base; same-file base chains allowed when proven in-file; cross-file base chains emit nothing",
        "note": "Ten public virtual user callbacks/custom-serialization entry points (8 OnStart*/OnStop* lifecycle + OnSerialize/OnDeserialize) PLUS the two protected virtual SyncVar-serialization overrides SerializeSyncVars/DeserializeSyncVars — the Weaver only generates those two when the subclass does NOT declare them (NetworkBehaviourProcessor.cs ~406-420/676-690), so a user override is retained and invoked via OnSerialize/OnDeserialize (NetworkBehaviour.cs ~1219-1235). EXCLUDED by role: OnValidate (already a MonoBehaviour message), Weaved (public virtual but Weaver-injected processed-marker), showSyncMethod (internal editor affordance)."
      }
    },
    "attributeEntryPoints": {
      "note": "Same semantics as fishnet.attributeEntryPoints: the decorated INSTANCE method gets entry-point liveness (self-reference), requires the declaring class provably NetworkBehaviour-based in the SAME file AND the file gate open. Static methods emit nothing. ACCURACY: Mirror user code CALLS these methods directly — the Weaver moves the original body aside and rewrites the call site's target body into a network send (MethodProcessor.cs) — so RPC methods often ALSO have real in-code callers. The synthetic self-reference SUPPLEMENTS those (remote receive dispatch reaches the method even when no in-project call site exists); it does not assert the method lacks callers.",
      "methodAttributes": [
        { "attribute": "Command",   "scope": "instance method", "invokedBy": "client→server remote call (Weaver-rewritten; requiresAuthority variants irrelevant to liveness)", "requiresInstance": true },
        { "attribute": "ClientRpc", "scope": "instance method", "invokedBy": "server→all-clients remote call", "requiresInstance": true, "collidesWith": "Unity.Netcode ClientRpcAttribute (gated out by disqualifyingUsingPrefixes)" },
        { "attribute": "TargetRpc", "scope": "instance method", "invokedBy": "server→single-client remote call", "requiresInstance": true, "collidesWith": "FishNet TargetRpcAttribute (gated out by disqualifyingUsingPrefixes)" }
      ],
      "referenceKind": "entry-point liveness (no target); supplements possible direct in-code callers"
    },
    "syncVarFields": {
      "note": "Mirror consumes EVERY non-static field bearing [SyncVar], hook or not: the Weaver enumerates fields, recognizes SyncVarAttribute, records and rewrites each (SyncVarAttributeProcessor.cs ~445-483; static fields are a Weaver error, ~455-465). CONSUMED RULE: when the file gate is open AND the declaring class is provably NetworkBehaviour-based in the same file, a non-static [SyncVar] field gets framework-consumed field liveness — same mechanism as the existing serialization-contract field rule (the `unity:field:` reference unity.ts already emits for serialized fields, ~848-860). Static [SyncVar] fields emit nothing.",
      "attribute": "SyncVar",
      "appliesTo": "non-static fields",
      "referenceKind": "field liveness (framework-consumed serialized field)"
    },
    "syncVarHooks": {
      "note": "[SyncVar(hook = nameof(Method))] / [SyncVar(hook = \"Method\")] names a change-callback resolved by Mirror's Weaver at compile time from the plain string (SyncVarAttributeProcessor.FindHookMethod). String-name dispatch — the hook method otherwise looks dead. CONSUMED RULE: gate open AND declaring class provably NetworkBehaviour-based same-file AND the hook string names a method with EXACTLY ONE declaration in the SAME class block → emit a reference keeping that method live (the field's syncVarFields liveness is separate and unconditional). EMIT NOTHING when: hook value is not a compile-time simple-identifier string/nameof (qualified forms like nameof(Other.M) included), the name is absent from the class block, the name is OVERLOADED in the class block (Mirror legally picks the 2-param signature match — we deliberately miss rather than guess; documented coverage bound), or NetworkBehaviour-ness is only provable cross-file. DOCUMENTED COVERAGE BOUNDS (deliberate missed edges, per emit-nothing): overloaded hook names; hooks whose field and method live in different partial-class blocks/files; derived-class overrides of a virtual hook (Weaver emits ldvirtftn — runtime dispatch we don't model). Mirror's own lookup searches ONLY the declaring type's methods (Extensions.cs GetMethods reads td.Methods; the parent-walking helper is a different function), so same-class-block matching is aligned with the Weaver, not narrower.",
      "attribute": "SyncVar",
      "argumentName": "hook",
      "hookScope": "same class block, same file, unique non-overloaded name",
      "weaverSource": "Assets/Mirror/Editor/Weaver/Processors/SyncVarAttributeProcessor.cs @ v96.10.3"
    },
    "excludedRows": {
      "note": "Present in Mirror v96.10.3 but deliberately NOT consumed as liveness rows, per the emit-nothing policy. Deferrals are recorded, not implied-covered.",
      "guardAttributes": {
        "rows": ["Server", "ServerCallback", "Client", "ClientCallback"],
        "reason": "Guard attributes — Weaver injects an early-return/warning wrapper; they do not change WHO calls the method, only whether it may run. Not framework entry points."
      },
      "editorAttributes": {
        "rows": ["Scene", "ShowInInspector", "ReadOnly", "WeaverPriority"],
        "reason": "Editor/inspector affordances and Weaver-internal reader/writer priority — no runtime dispatch of user methods."
      },
      "deferredHostBases": {
        "rows": ["NetworkManager", "NetworkRoomManager", "NetworkRoomPlayer", "NetworkAuthenticator", "InterestManagement", "InterestManagementBase"],
        "reason": "Real framework-invoked virtual surfaces (corpus: NetworkManager 32 virtuals, NetworkRoomManager 22, NetworkAuthenticator 6; Mirror calls authenticator overrides from NetworkManager.cs ~1155/1224), and all are MonoBehaviour-derived CROSS-FILE — the Tier-1 same-file base-chain rule cannot prove the chain from user code, and NetworkManager also collides by name with NGO's NetworkManager. DEFERRED as a candidate later row set (would need the same gate + per-base method lists); overrides of these virtuals in user subclasses still get normal C# override resolution when the base resolves. Do not treat as covered.",
        "candidateTests": "user subclass of NetworkManager overriding OnServerAddPlayer under an open mirror gate"
      },
      "messageHandlers": {
        "rows": ["NetworkServer.RegisterHandler<T>", "NetworkClient.RegisterHandler<T>", "NetworkClient.ReplaceHandler<T>"],
        "reason": "Handler is passed as a method-group ARGUMENT — codegraph's general C# function-as-value capture (#756, function-ref.ts call-argument branch) already resolves same-file handlers as references edges. A mirror-specific rule would double-emit. Recorded as covered-by-general-mechanism, not omitted."
      }
    },
    "eventSubscriptions": {
      "note": "SyncObject change callbacks in Mirror are plain C# Action delegate fields subscribed with `+=` (SyncList<T>: OnChange/Callback/OnAdd/OnInsert/OnSet/OnRemove/OnClear — 7; SyncSet<T> owns its 4 directly; SyncDictionary's 5 live on its BASE class SyncIDictionary<TKey,TValue>). Method-group references, NOT string dispatch — the general function-ref mechanism (`+=` RHS) already resolves same-file handlers. Emit nothing new; a mirror-specific bridge would double-emit. Same finding and coverage bound as fishnet.eventSubscriptions.",
      "resolverBehavior": "Emit nothing new; rely on the existing general C# function_ref mechanism.",
      "syncTypesWithActionCallbacks": ["SyncList", "SyncSet", "SyncIDictionary", "SyncDictionary", "SyncHashSet", "SyncSortedSet"]
    }
  },
  "catalogNotes": {
    "documentationOnly": true,
    "note": "Not read by unity.ts at runtime. Provenance, quarantine, deferrals, and false-positive guard record for behaviors hardcoded in the resolver.",
    "baseWhitelist": {
      "description": "The host-invoked-base whitelist. A UnityEngine/UnityEditor base not in this set does not make its methods host-invoked. Runtime equivalent: Object.keys(hostInvokedBases).",
      "bases": [
        "MonoBehaviour",
        "ScriptableObject",
        "StateMachineBehaviour",
        "Editor",
        "EditorWindow",
        "PropertyDrawer",
        "DecoratorDrawer",
        "AssetPostprocessor",
        "AssetModificationProcessor"
      ],
      "deferred": [
        "ScriptableWizard",
        "IPreprocessBuildWithReport / build interfaces",
        "PlayerLoop system entries"
      ]
    },
    "deferredPackageAttributes": {
      "networking": {
        "rows": ["ClientRpc", "Rpc", "Command", "SyncVar hook"],
        "reason": "Netcode-for-GameObjects (NGO), Mirror, and legacy UNet attributes; package-specific and name-colliding. [ClientRpc]/[Rpc] (NGO + Mirror), [Command] (Mirror/UNet), and Mirror's [SyncVar(hook=...)] have no FishNet-gated home in v1.",
        "graduated": "FishNet's ServerRpc, ObserversRpc, TargetRpc, Replicate, and Reconcile GRADUATED to the consumed top-level `fishnet` section, which fires only behind the per-file FishNet using-gate. NGO/Mirror ServerRpc/TargetRpc are still excluded here: the fishnet gate emits nothing in any file importing Unity.Netcode or Mirror.",
        "futureGate": "For the still-deferred NGO/Mirror/UNet rows: require package evidence such as using Unity.Netcode or using Mirror plus their NetworkBehaviour base; likely sibling gated sections mirroring the fishnet one."
      },
      "tests": {
        "rows": ["Test", "UnityTest", "SetUp", "TearDown", "OneTimeSetUp", "OneTimeTearDown", "UnitySetUp", "UnityTearDown"],
        "reason": "Package docs and collision-prone user attribute names; not core Unity 6.5 corpus rows.",
        "futureGate": "Same-file using NUnit.Framework or using UnityEngine.TestTools gate; v1.1 candidate."
      }
    },
    "quarantine": [
      { "row": "ShowButton", "source": "EditorWindow", "reason": "Real-but-undocumented magic method; absent from the local corpus page." },
      { "row": "OnHeaderGUI", "source": "Editor", "reason": "Absent from the local Editor corpus page." },
      { "row": "OnBeforeTransformParentChanged", "source": "MonoBehaviour", "reason": "UGUI/UIBehaviour-internal candidate; not on the MonoBehaviour corpus page." },
      { "row": "OnCanvasGroupChanged", "source": "MonoBehaviour", "reason": "UGUI/UIBehaviour-internal candidate; not on the MonoBehaviour corpus page." },
      { "row": "OnDidApplyAnimationProperties", "source": "MonoBehaviour", "reason": "UGUI/UIBehaviour-internal candidate; not on the MonoBehaviour corpus page." },
      { "row": "OnRectTransformDimensionsChange", "source": "MonoBehaviour", "reason": "UGUI/UIBehaviour-internal candidate; not on the MonoBehaviour corpus page." },
      { "row": "DrawDefaultInspector / DrawHeader / DrawPreview / CreatePreview / Repaint / ShouldHideOpenButton", "source": "Editor utility methods", "reason": "User-callable utilities, not host-invoked entry points." },
      { "row": "SaveChanges / DiscardChanges", "source": "Editor and EditorWindow", "reason": "User-callable utilities or marginal editor APIs, not v1 host callbacks." },
      { "row": "OnBackingScaleFactorChanged", "source": "EditorWindow", "reason": "Deferred/quarantined until corpus-backed." }
    ],
    "falsePositiveGuards": [
      { "id": "no-magic-method-without-host-base", "rule": "A method named Update/Start/OnEnable/etc. on a class whose base is not in the whitelist is a normal method; emit no host-invoked liveness. Bases are read only from class headers." },
      { "id": "same-file-base-chain-only", "rule": "Same-file base chains may emit when proven in-file; cross-file base chains emit nothing under the per-file extraction ceiling." },
      { "id": "static-modifier-enforcement", "rule": "Static-only attributes on non-static methods emit nothing; ContextMenu on static methods emits nothing." },
      { "id": "unique-overload-string-invoke", "rule": "String-invoked method references require exactly one same-class method match; overload ambiguity emits nothing." },
      { "id": "receiver-whitelist-send-message", "rule": "SendMessage/BroadcastMessage/SendMessageUpwards link only for implicit, this, or gameObject receivers and only to a unique same-class sibling; never project-wide name linking." },
      { "id": "no-computed-string-invoke", "rule": "Variables, concatenation, interpolation, dotted nameof, and empty strings emit nothing." },
      { "id": "no-typeof-builtin-reference", "rule": "typeof(X) and serialized field types skip primitive, builtin, System.*, UnityEngine.*-qualified, and known external types." },
      { "id": "attribute-token-exact", "rule": "Match attributes by exact token with optional Attribute suffix and UnityEngine./UnityEditor. qualification; no substring matches such as MenuItemCustom." }
    ],
    "dynamicOrOutOfScope": [
      { "id": "reflection-invoke", "apiPattern": "MethodInfo.Invoke / Type.GetType(\"...\") / Activator.CreateInstance", "resolverBehavior": "Emit nothing." },
      { "id": "addcomponent-string", "apiPattern": "gameObject.AddComponent(\"TypeName\") (deprecated string form)", "resolverBehavior": "Emit nothing in v1 unless separately proven." },
      { "id": "generic-getcomponent", "apiPattern": "GetComponent<T>() / AddComponent<T>()", "resolverBehavior": "Generic type args are ordinary static type references handled by C# extraction; do not double-emit." }
    ],
    "corpusVerification": {
      "callbacksAttributes": {
        "status": "complete",
        "result": "kept",
        "verifiedPages": [
          "Callbacks.DidReloadScripts",
          "Callbacks.PostProcessBuildAttribute",
          "Callbacks.PostProcessSceneAttribute",
          "Callbacks.OnOpenAssetAttribute"
        ]
      },
      "monoBehaviourRectTransformPair": {
        "status": "complete",
        "kept": "OnChildRectTransformDimensionsChange",
        "quarantined": "OnRectTransformDimensionsChange",
        "result": "Local corpus has MonoBehaviour.OnChildRectTransformDimensionsChange page/xref and Messages row; no page, xref, or search hit for MonoBehaviour.OnRectTransformDimensionsChange."
      }
    },
    "tier2Deferred": {
      "description": "Unity-only asset wiring; not in v1. Requires parsing non-code YAML assets and/or the future Unity Slice Pack static scanner.",
      "items": [
        "UnityEvent inspector wiring: scene/.prefab/.asset YAML m_PersistentCalls to method.",
        "Animation events: .anim asset functionName to method by name.",
        "MonoBehaviour script usage: scene/prefab m_Script GUID to script asset.",
        "Public-field implicit serialization.",
        "DrawGizmo first-parameter type inference.",
        "Dotted nameof(Class.M).",
        "UIBehaviour/UGUI callback rows.",
        "Editor callback-registration APIs such as EditorApplication.update += fn."
      ]
    },
    "resolverCoverageNotes": [
      "v1 Tier 1 is code-only: base+magic-method liveness, serialization-attribute field/property liveness plus type refs, attribute entry-point liveness, typeof type refs, and conservative string-invoked sibling-method links.",
      "Cross-file base chains, computed strings, reflection, package-specific attributes, and all Tier 2 asset wiring emit nothing.",
      "Pending-corpus-verification rows must be verified or quarantined before unity.ts consumes them."
    ]
  }
}
